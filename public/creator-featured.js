(()=>{const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];let credits=null,signedIn=false,type='someone',genre='Acoustic Pop',voice='Female',own=false;const screens=['home','type','details','own','lyricsScreen','music','results','mySongs','gallery'];const pagePath=location.pathname.replace(/\/+$/,'');const featuredPage=pagePath==='/featuredsongs'||pagePath==='/featuredsongs.html';const mySongsPage=pagePath==='/mysongs';if(featuredPage){document.addEventListener('click',e=>{const b=e.target.closest('.gallery-filter button');if(!b)return;e.preventDefault();document.querySelectorAll('.gallery-filter button').forEach(x=>x.classList.remove('sel'));b.classList.add('sel');const kind=b.dataset.filter||'all';document.querySelectorAll('#gallery .song-card').forEach(card=>{card.style.display=(kind==='all'||card.dataset.kind===kind)?'':'none'});});}if(featuredPage){document.addEventListener('click',e=>{const arrow=e.target.closest('.gallery-scroll');if(!arrow)return;e.preventDefault();const row=document.querySelector('.gallery-filter');if(!row)return;row.scrollBy({left:arrow.classList.contains('gallery-prev')?-Math.max(220,row.clientWidth*.7):Math.max(220,row.clientWidth*.7),behavior:'smooth'});});}let selectedVersion=null,previousLyrics='';const cfg={someone:['Tell us about this special person','Names, memories and little details make the song personal.',true,'Who is this song for?','What would you like the song to say?'],wedding:['Tell us your love story','Give us the moments that belong in your wedding or anniversary song.',true,'Who is the song celebrating?','Tell us about the love story'],celebration:["What are we celebrating?",'Turn an achievement or milestone into a song worth remembering.',true,'Who are we celebrating?','Tell us about the achievement'],faith:['What message should the song carry?','Create an uplifting song around hope, gratitude, encouragement or faith.',false,'','What is the heart of the song?'],story:['Tell us your story','Your experience can become the heart of an original song.',false,'','What happened, and what should the song say?'],original:['Start with your idea','Any topic or concept can be the beginning of a song.',false,'','What do you want your song to be about?'],jingle:['Create a short, catchy jingle','Perfect for a brand, business, product, service or campaign.',false,'','What should the jingle promote or communicate?']};function show(x){if(x==='gallery'&&!featuredPage){location.href='/featuredsongs';return}if(x==='home'&&(featuredPage||mySongsPage)){location.href='/';return}screens.forEach(s=>$('#'+s).classList.toggle('on',s===x));scrollTo({top:0,behavior:'smooth'})}function toast(x){let t=$('#toast');t.textContent=x;t.classList.add('show');clearTimeout(t.x);t.x=setTimeout(()=>t.classList.remove('show'),2600)}async function syncWallet(){try{const r=await fetch('/api/account',{cache:'no-store',credentials:'same-origin'}),d=await r.json();signedIn=!!d.signedIn;credits=signedIn?Number(d.account?.credits?.total||0):null;return d}catch(e){console.error('MQ3 wallet sync failed:',e);signedIn=false;credits=null;return null}}function openAccount(){document.querySelector('#account-button')?.click()}function needCredits(cost){if(!signedIn){toast('Sign in to your MQ3 account first.');openAccount();return false}if(!Number.isFinite(credits)||credits<cost){toast(`You need ${cost} MQ3 Credits. Load Credits to continue.`);window.dispatchEvent(new CustomEvent('mq3-open-credit-load',{detail:{creditsNeeded:cost}}));return false}return true}window.addEventListener('mq3-wallet-updated',()=>syncWallet());syncWallet();$('#mySongsHome').onclick=async e=>{e.preventDefault();await syncWallet();if(!signedIn){toast('Sign in to see your songs.');openAccount();return}location.href='/mysongs'};$$('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go));$$('[data-type]').forEach(b=>b.onclick=()=>{type=b.dataset.type;if(type==='own'){own=true;show('own');return}own=false;$('#musicBack').dataset.go='lyricsScreen';let c=cfg[type];$('#detailTitle').textContent=c[0];$('#detailSub').textContent=c[1];$('#person').classList.toggle('hidden',!c[2]);$('#name').required=c[2];$('#nameLabel').childNodes[0].nodeValue=c[3];$('#storyLabel').textContent=c[4];show('details')});async function aiLyrics(payload){const r=await fetch('/api/song-creator/lyrics',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||'AI lyrics could not be created.');return d}$('#form').onsubmit=async e=>{e.preventDefault();let conf=cfg[type],name=$('#name').value.trim(),story=$('#story').value.trim();if(!story||(conf[2]&&!name)){toast('Please complete the story details first.');return}const submit=e.submitter||$('#form button[type="submit"]');const label=submit?.textContent;if(submit){submit.disabled=true;submit.textContent='Writing your song…'}try{const d=await aiLyrics({mode:'create',songType:type,name,story,language:$('#language')?.value||'English',relationship:$('#rel')?.value||'',occasion:$('#occ')?.value||''});previousLyrics=$('#lyrics').value;$('#title').value=d.title;$('#lyrics').value=d.lyrics;show('lyricsScreen');toast('Your lyrics are ready. Review and edit anything you like.')}catch(error){toast(error.message)}finally{if(submit){submit.disabled=false;submit.textContent=label}}};$('#ownContinue').onclick=()=>{let t=$('#ownTitle').value.trim(),l=$('#ownLyrics').value.trim();if(!t||!l){toast('Add your song title and lyrics first.');return}$('#title').value=t;$('#lyrics').value=l;own=true;$('#musicBack').dataset.go='own';show('music')};$$('[data-chip]').forEach(b=>b.onclick=async()=>{const current=$('#lyrics').value.trim();if(!current){toast('There are no lyrics to rewrite.');return}const oldLabel=b.textContent;b.disabled=true;b.textContent='Writing…';try{const d=await aiLyrics({mode:b.dataset.chip,songType:type,name:$('#name')?.value?.trim()||'',story:$('#story')?.value?.trim()||'',language:$('#language')?.value||'English',relationship:$('#rel')?.value||'',occasion:$('#occ')?.value||'',title:$('#title').value,lyrics:current});previousLyrics=current;$('#title').value=d.title;$('#lyrics').value=d.lyrics;toast('New lyrics ready. You can restore the previous version anytime.')}catch(error){toast(error.message)}finally{b.disabled=false;b.textContent=oldLabel}});$('#restoreLyrics').onclick=()=>{if(!previousLyrics){toast('No previous lyrics version yet.');return}const current=$('#lyrics').value;$('#lyrics').value=previousLyrics;previousLyrics=current;toast('Previous lyrics restored.');};function choose(id,set){$$(id+' button').forEach(b=>b.onclick=()=>{$$(id+' button').forEach(x=>x.classList.remove('sel'));b.classList.add('sel');set(b.dataset.v)})}choose('#genres',v=>{genre=v;$('#customStyleWrap').classList.toggle('hidden',v!=='Custom')});choose('#voices',v=>voice=v);$('#generate').onclick=async()=>{await syncWallet();if(!needCredits(50))return;let finalGenre=genre==='Custom'?($('#customStyle').value.trim()||'Custom Style'):genre;$('#resultTitle').textContent=$('#title').value||'Your MQ3 Song';$('#a').textContent=`${finalGenre} · ${voice}`;$('#b').textContent=`${finalGenre} · ${voice} · Alternate`;selectedVersion=null;$$('.result-card').forEach(x=>x.classList.remove('selected'));$$('.pick').forEach((x,i)=>x.textContent='Select Version '+(i?'B':'A'));$('#download').classList.add('inactive');$('#download').textContent='Select a Version to Download ↓';show('results');toast('Prototype preview created. No real Credits were charged.')};$('#again').onclick=async()=>{await syncWallet();if(!needCredits(50))return;toast('Music API is not connected yet, so no Credits were charged.')};$$('.pick').forEach(b=>b.onclick=()=>{selectedVersion=b.dataset.pick;$$('.result-card').forEach(x=>x.classList.toggle('selected',x.dataset.version===selectedVersion));$$('.pick').forEach(x=>x.textContent=x.dataset.pick===selectedVersion?'✓ Selected':'Select Version '+x.dataset.pick);$('#download').classList.remove('inactive');$('#download').textContent='Download Selected Song · Version '+selectedVersion+' ↓';});$('#download').onclick=()=>{if(!selectedVersion){toast('Choose Version A or B first.');return}toast('Version '+selectedVersion+' download activates when the music API is connected.');};$('#featureSong').onchange=e=>$('#featureOptions').classList.toggle('hidden',!e.target.checked);let catalogSongs=[];
function galleryKind(song){
  const exactTitle=String(song.title||'').trim().toLowerCase();
  if(exactTitle==='joan')return 'someone';
  if(exactTitle==='ikaw na nga')return 'opm';
  const cat=String(song.category||'').trim().toUpperCase();
  const text=[song.title,song.names,song.lyrics].filter(Boolean).join(' ').toLowerCase();
  if(cat.includes('NAME'))return 'someone';
  if(cat==='OPM'||cat.includes('OPM'))return 'opm';
  if(cat.includes('INSPIRATIONAL'))return 'faith';
  const wedding=/\b(wedding|anniversary|marry|married|marriage|bride|groom|husband|wife|vow|vows|altar|aisle|years together|our wedding|kasal|anibersaryo|pangako)\b/i;
  const celebration=/\b(birthday|graduation|graduate|congratulations|congrats|achievement|achieve|success|retirement|retire|celebrate|celebration|milestone|victory|winner|proud of you|kaarawan|tagumpay|pagtatapos)\b/i;
  const faith=/\b(god|lord|jesus|faith|pray|prayer|grace|blessing|blessed|worship|heaven|holy|savior|creator|miracle|diyos|panginoon|panalangin|biyaya|pag-asa|pagpapala)\b/i;
  const love=/\b(love|in love|my heart|forever|together|romance|darling|sweetheart|kiss|hold you|miss you|my girl|my boy|mahal|pag-ibig|puso|sinta|irog|sa'yo lang|sayo lang)\b/i;
  if(wedding.test(text))return 'wedding';
  if(celebration.test(text))return 'celebration';
  if(faith.test(text))return 'faith';
  if(love.test(text))return 'love';
  return 'story'
}
function openCatalogSong(song){const match=String(song.suno_url||'').match(/^https:\/\/suno\.com\/song\/([a-f0-9-]{36})$/i);if(!match){toast('This song player is unavailable.');return}let d=$('#catalogPlayer');if(!d){d=document.createElement('dialog');d.id='catalogPlayer';d.className='catalog-player';document.body.append(d);d.addEventListener('close',()=>d.replaceChildren())}d.replaceChildren();const heading=document.createElement('h2');heading.textContent=song.title||'MQ3 Song';heading.className='catalog-player-title';const wrap=document.createElement('div');wrap.className='catalog-embed-wrap';const frame=document.createElement('iframe');frame.src='https://suno.com/embed/'+match[1];frame.title=(song.title||'MQ3 Song')+' player';frame.allow='autoplay; encrypted-media; fullscreen';frame.referrerPolicy='strict-origin-when-cross-origin';frame.setAttribute('sandbox','allow-scripts allow-same-origin');wrap.append(frame);const brand=document.createElement('div');brand.className='catalog-brand';const brandLogo=document.createElement('img');brandLogo.src='/logo.png';brandLogo.alt='MQ3';const brandCopy=document.createElement('div');brandCopy.className='catalog-brand-copy';const brandTitle=document.createElement('strong');brandTitle.innerHTML='Song <em>Creator</em>';const brandTag=document.createElement('small');brandTag.textContent='TURN IDEAS INTO MUSIC';brandCopy.append(brandTitle,brandTag);brand.append(brandLogo,brandCopy);const close=document.createElement('button');close.type='button';close.className='catalog-close';close.textContent='Close Player';close.onclick=()=>d.close();const lyrics=document.createElement('details');lyrics.className='catalog-lyrics';lyrics.open=true;const summary=document.createElement('summary');summary.textContent='Lyrics';const body=document.createElement('div');body.className='catalog-lyrics-body';body.textContent=song.lyrics||'Lyrics have not been added yet.';lyrics.append(summary,body);d.append(brand,wrap,close,lyrics);d.showModal()}
function renderCatalog(songs){const list=$('#gallery .gallery-list');if(!list)return;list.replaceChildren();const sortedSongs=[...songs].sort((a,b)=>String(a.title||'').localeCompare(String(b.title||''),undefined,{sensitivity:'base',numeric:true}));sortedSongs.forEach(song=>{const card=document.createElement('article');card.className='song-card';card.dataset.kind=galleryKind(song);card.dataset.songId=song.id;const cover=document.createElement('div');cover.className='cover';cover.textContent='♪';const info=document.createElement('span');const cat=document.createElement('small');cat.textContent=String(song.category||'MQ3 SONG').replaceAll('_',' ');const title=document.createElement('b');title.textContent=song.title||'Untitled Song';const meta=document.createElement('em');meta.textContent=song.names||'MQ3 Music';info.append(cat,title,meta);const wave=document.createElement('div');wave.className='song-wave';const titleLen=String(song.title||'').trim().length;wave.classList.add(titleLen<=8?'wave-long':titleLen<=18?'wave-medium':'wave-short');for(let i=0;i<30;i++){const bar=document.createElement('i');bar.style.setProperty('--n',String((i*7)%13));bar.style.setProperty('--d',String(i));wave.append(bar)};const play=document.createElement('button');play.className='mini-play';play.type='button';play.textContent='▶';play.setAttribute('aria-label','Play '+(song.title||'song'));play.onclick=()=>openCatalogSong(song);const buy=document.createElement('button');buy.className='buy';buy.type='button';buy.textContent='Get MP3 + Lyrics · 50 credits';buy.onclick=async()=>{await syncWallet();if(!needCredits(50))return;toast('Catalog purchase connection is next. No Credits were charged.')};card.append(cover,info,wave,play,buy);list.append(card)});filterGallery()}
async function loadCatalog(){try{const r=await fetch('/api/catalog',{credentials:'same-origin'});if(!r.ok)throw Error('Catalog unavailable');const d=await r.json();catalogSongs=Array.isArray(d.songs)?d.songs:[];renderCatalog(catalogSongs);const note=$('#gallery .note');if(note)note.textContent=catalogSongs.length?catalogSongs.length+' published MQ3 songs loaded from your existing catalog.':'No published songs are available yet.'}catch(e){console.error('MQ3 catalog load failed:',e);toast('Featured Songs could not be loaded right now.')}}
if(featuredPage){show('gallery')}if(mySongsPage){syncWallet().then(()=>{if(!signedIn){show('home');toast('Sign in to see your songs.');openAccount()}else show('mySongs')})}
let galleryFilter='all';loadCatalog();function filterGallery(){const q=($('#gallerySearch')?.value||'').trim().toLowerCase();const list=$('#gallery .gallery-list');if(list){[...list.querySelectorAll('.song-card')].sort((a,b)=>{const at=(a.querySelector('b')?.textContent||'').trim();const bt=(b.querySelector('b')?.textContent||'').trim();return at.localeCompare(bt,undefined,{sensitivity:'base',numeric:true});}).forEach(card=>list.appendChild(card));}$('.song-card').forEach(c=>{const categoryOk=galleryFilter==='all'||c.dataset.kind===galleryFilter;const searchOk=!q||c.textContent.toLowerCase().includes(q);c.classList.toggle('hidden-card',!(categoryOk&&searchOk));});}document.addEventListener('click',e=>{const b=e.target.closest('#gallery .gallery-filter button');if(!b)return;e.preventDefault();$('#gallery .gallery-filter button').forEach(x=>x.classList.remove('sel'));b.classList.add('sel');galleryFilter=b.dataset.filter||'all';filterGallery();});const gallerySearch=$('#gallerySearch');if(gallerySearch)gallerySearch.addEventListener('input',filterGallery);$$('.buy').forEach(b=>b.onclick=async()=>{await syncWallet();if(!needCredits(50))return;toast('Catalog purchase is not connected on this screen yet. No Credits were charged.')});$$('.play,.mini-play').forEach(b=>b.onclick=()=>toast('Audio preview activates when the real song catalog/music API is connected.'))})();